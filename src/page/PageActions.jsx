import React from 'react';

const propTypes = {};

const PageActions = ({ children }) => <div />;
const Action = ({
  actionKey, label, isDisabled, onSelect, refCallback,
}) => <div />;

PageActions.propTypes = propTypes;

export default PageActions;
export { Action };
