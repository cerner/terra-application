import ApplicationEventEmitter from '../../../src/application-event-emitter';

describe('application-event-emitter/index', () => {
  it('should export ApplicationEventEmitter', () => {
    expect(ApplicationEventEmitter).toBeDefined();
  });
});
